# StarX 0.4.8

`starx-universal-0.4.8.jar`

## 修复

- 已确认但过期的跨设备绑定挑战不能再被执行。
- 账号注销会删除网站绑定，避免残留网站信任关系。
- Premium 和 Floodgate 自动登录分别受对应 bypass 开关控制。
- QQ/Discord 绑定发生归属冲突时返回失败，不再误报绑定成功。

## 验证

- common 与 velocity 主代码、测试代码编译通过。
- 定向测试被本机 Gradle worker 环境阻断，未宣称测试通过。
