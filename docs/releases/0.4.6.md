# StarX 0.4.6

`starx-universal-0.4.6.jar`

## 修复

Velocity 不再把未绑定网站的本地老账号送入网页登录确认。高风险登录只有在账号存在已验证网站绑定时才使用网页审批；没有网站绑定时继续走本地密码认证，启用 TOTP 的账号仍按 TOTP 流程处理。

## 升级

替换 `starx-universal.jar` 后定向重启 Velocity，并确认启动日志加载 `starx 0.4.6`。
