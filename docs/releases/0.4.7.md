# StarX 0.4.7

`starx-universal-0.4.7.jar`

## 修复

- 未完成已验证网站绑定的本地账号不能进入 `/login web` 等待确认。
- 全局关闭 TOTP 时，高风险登录会根据已验证网站绑定回退到网页审批；未绑定账号继续使用本地认证。
- 认证异步派发异常、网页登录审批超时后统一清理认证状态和认证世界会话。
- 管理端网站绑定状态改为读取已验证绑定；正版免密开关现在参与实际准入判断。

## 升级

替换 `starx-universal.jar` 后定向重启 Velocity，并确认启动日志加载 `starx 0.4.7`。
