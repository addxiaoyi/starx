# StarX 0.4.4

## 管理员密码修改

Velocity 控制台或具备 `starx.password.reset` 权限的管理员可以执行：

```text
/sxadmin setpassword <玩家> <新密码>
```

命令会复用 StarX 认证服务进行密码校验和 BCrypt 哈希，并撤销该玩家现有的可信认证记录。密码不会写入命令反馈或日志。密码参数请避免出现在共享控制台记录中。

## 升级说明

替换 Velocity 上的 `starx-universal-0.4.4.jar` 后重启代理。旧版 JAR 请保留备份，确认命令可用后再清理。
