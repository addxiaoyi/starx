# StarX 1.0.13

下载附件：`starx-universal-1.0.13.jar`

## 配置

新增插件数据目录下的 `cross-server-tab.yml`：

```yaml
servers:
  lobby: "大厅"
  survival: "生存服"
  minigames: "小游戏"
```

键必须使用 `velocity.toml` 的真实服务器名；未配置的服务器显示真实注册名。
